# 引継ぎメモ：キャラクター図鑑（AIイラスト プロンプト管理ツール）

## これは何か
AIイラスト生成（SDXL系サービス想定）で作ったキャラクターの
「サムネイル画像」「プロンプト」「ネガティブプロンプト」「モデル/LoRA/シード等の生成設定」
「自由タグ」をまとめて登録・検索・バックアップできる、個人用の管理ツール。

- 利用者：本人専用（他人と共有するデータベースではない）
- 公開方法：GitHub Pages を想定（静的1ファイルで動く）
- 現状の実体：`index.html` 1ファイルのみ（HTML + CSS + JS すべて内包、ビルド不要、外部ライブラリ依存なし）

## 現状のファイル構成
```
index.html   ← これ1本で完結する静的サイト
```
Googleフォント（Zen Maru Gothic / Noto Sans JP）だけを外部から読み込む。それ以外は完全にオフラインで動作する。

## データの保存場所（重要）
- 登録データ本体：ブラウザの **IndexedDB**（DB名 `character_library_db`、ストア名 `entries`）
- カテゴリ一覧・自動判定ON/OFF設定：**localStorage**
  - `char_lib_categories_v1`
  - `char_lib_auto_classify_v1`

**端末・ブラウザごとに別データ**になる。サーバー側に集約する仕組みは無い。
これは要件として確認済み（本人専用ツールでよい、という合意）。
キャッシュ削除・シークレットモード・別ブラウザ・別端末では**データは引き継がれない**。

対策として、画面右上に **JSONエクスポート/インポート機能**を実装済み：
- 「📤 書き出し」：全登録内容 + カテゴリ一覧 + 自動判定設定 を1つのJSONにしてダウンロード
- 「📥 読み込み」：JSONを選択すると件数を表示した上で「既存に追加（マージ）」/「全て置き換え」を選べる

## 実装済み機能一覧
1. **一覧画面**
   - サムネイル付きカードグリッド
   - 名前・タグ・モデル名でのキーワード検索
   - 並び替え（登録日 新しい順/古い順、名前 昇順/降順）
   - タグチップによる絞り込み（複数選択でOR条件）
2. **新規登録・編集フォーム**
   - サムネイル画像（アップロード時に長辺640pxへリサイズ→JPEG base64でDB保存）
   - 名前
   - キャラクタータグ（自由入力、Enterで追加、一覧の絞り込みに使用）
   - **プロンプトのタグ分割＆カテゴリ分類**
     - プロンプト全文を貼り付けて「タグに分割して追加」→ カンマ区切りで分解
       - 括弧 `()[]{}` の中のカンマは無視する分割ロジック（重み表記 `(masterpiece:1.2)` などが壊れない）
     - カテゴリは自由に追加・削除可能（初期値：基本/画質・人物・表情・服装・ポーズ・背景・その他）
     - 各タグはプルダウンでカテゴリを手動選択できる
     - **キーワード辞書によるルールベース自動判定**（例：`shirt`→服装、`background`→背景）。既定のカテゴリ名に一致する場合のみ機能する簡易機能。ON/OFF切替あり、「未分類のタグを再判定」ボタンあり
     - 手動で単発タグを追加する入力欄もあり
     - 全文プレビュー＋コピー機能
   - ネガティブプロンプト（プレーンテキスト、分類なし）
   - 生成設定を項目別に入力：モデル、サンプラー、ステップ数、CFG Scale、シード値、サイズ（縦横比）、生成時間
   - LoRA（名前＋強さのペアを複数行、追加/削除可能）
   - メモ欄（自由記述）
3. **詳細画面**
   - プロンプトはカテゴリごとにグループ化して表示、全文コピー可
   - ネガティブプロンプトのコピー
   - 生成設定を2列グリッドで表示、LoRAはチップ表示
   - 編集・削除（削除は確認ダイアログあり）
4. **旧データ互換**
   - 過去バージョン（プロンプトがプレーン文字列だった頃）のデータを開いた場合、自動でタグ分割し「未分類」として扱う `migrateEntry()` 関数あり

## デザイン
- フォント：見出し `Zen Maru Gothic`、本文 `Noto Sans JP`
- ダーク/ライテーマ両対応（`prefers-color-scheme` + `data-theme` 属性の両方に対応したCSS変数設計）
- アクセントカラー：ローズ系（`--accent`）

## 未解決・今後の検討事項
- **デプロイ方法**：一度 `gh-pages`（npmパッケージ）経由のデプロイでエラーが出た経緯があるが、原因調査は一旦保留にしている（このツール自体はビルド不要な単一HTMLなので、本来 `gh-pages` は不要で、GitHubにpushしてPages設定するだけで公開可能）。もし別のフレームワーク（Vite等）でラップする方針に変える場合は要件から再検討が必要。
- 自動判定の辞書は簡易的な部分一致ロジックであり、精度は高くない。ユーザーがカテゴリ名を独自のものに変更すると機能しなくなる制約がある。
- 複数人での共有・同期は現状スコープ外（要件として個人利用に確定済み）。将来的に共有したくなった場合はバックエンド（DB・認証）が別途必要になる。
- 自動テストは無し。

## Claude Codeでの引継ぎにあたって
- 作業フォルダ： `G:\Claude\character-library`（想定）
- 既存の `index.html` をこのフォルダに配置してスタート地点とする
- 単一HTMLのままGitHub Pages運用を続けるか、開発しやすいようにファイル分割（CSS/JS切り出し、ビルドツール導入）するかは、要件と相談しながら決めてよい
